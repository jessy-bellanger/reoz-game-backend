/**
 *
 * @param name {string}
 * @param behaviour {Behaviour}
 * @constructor
 */
function Item(name, behaviour) {
    this.name      = name;
    this.behaviour = behaviour;
}

module.exports = Item;